export type { WorldShape } from '../../../../../utils/world-shape';
/** Physical world side length in meters. */
export type WorldSize = number;

export const MIN_WORLD_SIZE = 100;
export const MAX_WORLD_SIZE = 10_000;
